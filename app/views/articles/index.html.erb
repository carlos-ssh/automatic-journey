<h1>Articles</h1>
<p>Check out our new Articles</p>

<%= link_to 'Create new article', new_article_path %>

<% @articles.each do |a| %>
    <p><%= a.title %></p>
    <p><%= a.body %></p>
    <p><%= a.active %></p>
    <%= link_to 'Show', article_path(a) %>
<% end %>
